const path = require('path');

module.exports = {
    env: process.env.DATABASE_ENV || 'xxx',
    config: path.resolve('configs', 'database.js'),
    'migrations-path': path.resolve('database','migrations'),
    'seeders-path': path.resolve('database','seeders')
};